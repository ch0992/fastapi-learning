# 데이터베이스 설정
# SQLite 데이터베이스 사용, 현재 디렉토리에 sql_app.db 파일로 저장
DATABASE_URL=sqlite:///./sql_app.db

# 보안 설정
# JWT 토큰 생성에 사용될 비밀키 (실제 배포 시 보안을 위해 변경 필요)
SECRET_KEY=your-secret-key-here
# JWT 토큰 암호화 알고리즘 (HMAC + SHA256)
ALGORITHM=HS256
# JWT 토큰 만료 시간 (30분)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API 설정
# API 버전 경로 (/api/v1 형식으로 사용)
API_V1_STR=/api/v1
# 프로젝트 이름 (API 문서에 표시됨)
PROJECT_NAME=Advanced Book Management System

# 초기 관리자 계정 설정
# 초기 관리자 이메일 (실제 배포 시 변경 필요)
FIRST_SUPERUSER=admin@example.com
# 초기 관리자 비밀번호 (실제 배포 시 변경 필요)
FIRST_SUPERUSER_PASSWORD=admin123
